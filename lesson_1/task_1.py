# the user inputs 3-digits number
# to get each digit as a, b, c as int
a, b, c = map(int, input('Enter the 3-digit number ').strip())
# to find sum of all numbers and print it
cur_sum = a + b + c
print(f'{cur_sum = }')
# to find product of all numbers and print itcur_sum =
cur_product = a * b * c
print(f'{cur_product = }')